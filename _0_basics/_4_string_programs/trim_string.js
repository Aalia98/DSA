// TODO: Trim a String at the Beginning with trimStart() Method

const str = "   GeeksforGeeks";
let newStr = str.trimStart();

console.log("Modified String: " + "'" + newStr + "'");

// TODO: Trim a String at the End using trimEnd() Method

const str1 = "GeeksforGeeks   ";
let newStr1 = str1.trimEnd();

console.log("Modified String: " + "'" + newStr1 + "'");

// TODO: Trimming the string from both the ends

const word = " geeksforgeeks ";
console.log("Initial String: " + "'" + word + "'");

let new_word = word.trim();
console.log("Modified String: " + "'" + new_word + "'");
